"""Golden-question evaluation harness for the text-to-SQL agent.

This is a *tool*, not library code shipped as part of the
``universal_text2sql`` package — see ``eval/cli.py`` (run via
``python -m eval.cli``) and the "Evaluation" section of the README.
"""
